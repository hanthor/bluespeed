extensions:
  - name: linux-mcp-server
    type: stdio
    command: /home/linuxbrew/.linuxbrew/bin/linux-mcp-server
    env:
      LINUX_MCP_USER: REPLACE_WITH_YOUR_USERNAME
    # TODO: Phase 4 - Research and add dosu remote MCP support for Goose
    # dosu MCP configuration pending Goose remote MCP server support research
